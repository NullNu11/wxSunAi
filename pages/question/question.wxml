<!-- pages/question.wxml -->
 <view>问卷</view>
<!-- <view wx:for="{{questionName}}"  wx:for-item="value" wx:for-index="key">
    <view>{{key}}  {{value}} </view>  
</view> -->
<view wx:for-items="{{questionName}}" wx:for-item="item">
   <text >{{index}}</text>
   <view >{{item}}</view>
</view>